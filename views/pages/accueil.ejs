<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head') %>
</head>

<body>
    <header>
        <%- include('../partials/header') %>
    </header>
    <section class="intro flex-column">
        <h2 class="mt-4 d-block text-center">Welcome tech-watch user !</h2>
        <div class="block-text-intro d-block">
            <p class="text-intro">On this page you could take a look to the next tech-watch we will assist. You can find
                witch students will have to do the next
                presentation and when.</p></br>
            <p class="text-intro">Also, as you can see, above this section there is a menu. This menu offer you diverse
                options like: take a look about the tech-watch we
                had already assist; see the list of students availaible for do a tech-watch; a page for create the next
                tech-watch.</p></br>
            <p class="text-intro">Don’t be shy and try to go on diverses part of the website !</p>
        </div>
    </section>
    <section class="section-two-projects ">
        <ul class="ul-next-project">
            <% newProject.forEach(function(project) { %>
            <div class="rectangle">
                <li classe="li-subject">
                    <h3 class="text-center mt-2"><%= project.subject %></h3>
                </li>
                <div class="row mb-3">
                    <div class="col">
                        <li class="col">
                            <p class="p-students-affiliate d-inline">Students Affiliate :</p>
                            <% for(let i=0; i< project.students.length; i++) { %>
                            <p class="d-inline"><%= project.students[i].name %></p>
                            <% } %>
                        </li>
                    </div>
                    <div class="col text-right">
                        <li classe="li-date">
                            <p class="p-when d-inline">When:</p>
                            <p class="p-date d-inline"><%= project.date %></p>
                        </li>
                    </div>
                    <br />
                </div>
            </div>
            <% }); %>
        </ul>
    </section>
    <section>
        <h4>List of students they will must do a tech-watch soon:</h4>
    </section>
    <section class="colonnes justify-content-center">
        <% availableStudent.forEach(function(student) { %>
        <%= student.name %>
        <% }); %>
    </section>
    <footer>
        <%- include('../partials/footer') %>
    </footer>

</body>

</html>